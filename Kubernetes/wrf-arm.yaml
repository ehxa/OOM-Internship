apiVersion: kubeflow.org/v2beta1
kind: MPIJob
metadata:
  name: wrf-arm
spec:
  slotsPerWorker: 2
  runPolicy:
    cleanPodPolicy: Running
  mpiReplicaSpecs:
    Launcher:
      replicas: 1
      template:
        spec:
          containers:
          - image: ehxa/oom:ubuntu24.04-gcc-20250130-1250-configured
            name: wrf
            command: ["/bin/bash", "-c"]
            args:
              - |
                . /home/swe/wrf/gccvars.sh &&
                cd /home/swe/wrf/WRF/WRF/run/ &&
                cp /mnt/data/* . &&
                timeout 60s mpirun -np 4 ./wrf.exe || true &&
                echo "WRF job completed" &&
                echo "" &&
                echo "rsl.error.0000:" &&
                tail -n 15 rsl.error.0000 &&
                echo "rsl.out.0000:" &&
                tail -n 15 rsl.out.0000
            volumeMounts:
            - name: wrf-arm-volume
              mountPath: /mnt/data
          volumes:
          - name: wrf-arm-volume
            persistentVolumeClaim:
              claimName: wrf-arm-pvc

    Worker:
      replicas: 2
      template:
        spec:
          containers:
          - image: ehxa/oom:ubuntu24.04-gcc-20250130-1250-configured
            name: wrf
            command: ["/bin/bash", "-c"]
            args:
              - |
                . /home/swe/wrf/gccvars.sh &&
                sleep infinity
            volumeMounts:
            - name: wrf-arm-volume
              mountPath: /mnt/data
          volumes:
          - name: wrf-arm-volume
            persistentVolumeClaim:
              claimName: wrf-arm-pvc
